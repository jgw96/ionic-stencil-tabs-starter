{
  "name": "tabs-ionic",
  "private": true,
  "version": "0.0.1",
  "description": "tabs-ionic",
  "license": "MIT",
  "files": [
    "dist/"
  ],
  "scripts": {
    "build": "stencil build",
    "start": "stencil build --dev --watch --serve",
    "test": "stencil test --spec --e2e",
    "test.watch": "stencil test --spec --e2e --watch"
  },
  "dependencies": {
    "@ionic/core": "^4.6.0"
  },
  "devDependencies": {
    "@stencil/core": "^1.1.3"
  }
}
